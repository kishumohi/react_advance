import { userActions, userReducer } from './user/model/slice/userSlice';
import type { User, UserSchema } from './user/model/types/UserSchema';

export { userActions, userReducer };
export type { User, UserSchema };
