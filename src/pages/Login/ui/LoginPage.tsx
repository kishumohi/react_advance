import { Link } from 'react-router';

function LoginPage() {
    return (
        <div>
            LoginPage
            <Link to={'/'}>Home page</Link>
        </div>
    );
}

export default LoginPage;
